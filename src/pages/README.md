# src/pages

Route/page components for the app.

Files:

- `Index.tsx` - main landing page / index route.
- `NotFound.tsx` - fallback 404 page component.

Add page-level components here; route configuration (if any) references these files.
